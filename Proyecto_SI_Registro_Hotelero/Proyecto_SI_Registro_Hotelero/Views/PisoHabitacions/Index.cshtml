@model IEnumerable<Proyecto_SI_Registro_Hotelero.Models.PisoHabitacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Piso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Piso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PisoHId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PisoHId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PisoHId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
